import { Typography } from '@mui/material';

const Title = () => (
  <Typography
    variant="h4"
    color="primary"
    gutterBottom
    sx={{
      textAlign: 'center',
      marginTop: 8,
      fontSize: '64px',
    }}
  >
    PaperScraper
  </Typography>
);

export default Title;
